name: Knip

on:
  pull_request:

permissions:
  checks: write

jobs:
  run-knip:
    runs-on: ubuntu-latest
    steps:
      - name: Post the knip results
        uses: gitcoindev/knip-reporter@v1.2.1
        with:
          verbose: true
